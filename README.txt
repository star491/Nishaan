NISHAAN.AI AR FINAL

IMPORTANT: Serve this folder over HTTPS (GitHub Pages, Vercel, Netlify, or localhost). Do not open index.html directly.

FIRST BUTTON FIX:
The ENTER LIVE AR screen now transitions immediately on the user's tap, before camera startup finishes. Camera permission errors are shown explicitly instead of making the button appear dead. Rear-camera constraints have a generic-camera fallback.

The existing Glyph positions, moving real-human video holograms, floor-lock/recenter controls, and device-motion tracking are preserved.
